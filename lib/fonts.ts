/**
 * Local fonts from /public/fonts.
 * Add your .woff2 / .woff / .ttf files to public/fonts, then define them here.
 *
 * Example (once you have a file, add this and use fontSans.variable + fontSans.className in layout):
 *
 * import localFont from "next/font/local";
 * export const fontSans = localFont({
 *   src: "../public/fonts/YourSans.woff2",
 *   variable: "--font-sans",
 *   display: "swap",
 * });
 *
 * export const fontMono = localFont({
 *   src: "../public/fonts/YourMono.woff2",
 *   variable: "--font-mono",
 *   display: "swap",
 * });
 */

// Placeholder: system font until you add files to /public/fonts.
// When ready, use localFont as above; then use fontSans.variable on <html> and fontSans.className on <body>.
export const fontSans = {
  variable: "--font-sans",
  className: "font-sans",
};
